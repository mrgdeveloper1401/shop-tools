import Tickets from '@/component/templates/p-admin/Tickets/Tickets';
import { Suspense } from 'react';

const PageTickets = () => (
  <Suspense>
    <Tickets />
  </Suspense>
);

export default PageTickets;
