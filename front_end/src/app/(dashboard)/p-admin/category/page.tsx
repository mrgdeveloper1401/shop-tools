import CategorySection from '@/component/templates/p-admin/Category/Category';
import { Suspense } from 'react';

const CategoryPage = () => (
  <Suspense>
    <CategorySection />
  </Suspense>
);

export default CategoryPage;
